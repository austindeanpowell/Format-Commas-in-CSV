1. Make sure pip3 install pandas is installed
2. Place the Python Script and CSV File in the Same Folder
3. Ensure that the data-breach.csv file is in the same folder as this script.
4. Run Script ---> Open Terminal
5. Run in Terminal: python "yourfilename.py"
6. If you prefer not to overwrite the original file, change the cleaned data as "NewFileName.csv". in lower quadrant of file. 
